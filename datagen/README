experiments.cfg - main file for setting up the experiments.

csvgen.py - main script for generating .csv data based on the above set experiment. To be called with the
            argument which specifies the number of the experiment.

dataaug.m - main script for augmenting the training data.

artintersect.py - the script used for intersecting labels from two different raters.

---------------------------------------------------------
Experiment.py - the class for experiment parametrization. 